---
import { getCollection } from 'astro:content';
import BaseLayout from '../../../layouts/BaseLayout.astro';
import ArticleCard from '../../../components/ArticleCard.astro';

const posts = await getCollection('posts', ({ data }) => !data.draft);
const appliancePosts = posts.filter(p => p.data.subcategory === 'appliance')
  .sort((a, b) => new Date(b.data.publishDate).getTime() - new Date(a.data.publishDate).getTime());
---
<BaseLayout
  title="家電 レビュー"
  description="3Dプリンター・ロボット掃除機・空気清浄機・ドライヤーなどの家電個別レビュー記事一覧。"
>
  <section class="category-hero category-hero--appliance">
    <div class="container">
      <p class="breadcrumb"><a href="/categories/review/">個別レビュー</a> &gt; 家電</p>
      <h1 class="category-hero__title">🏠 家電 レビュー</h1>
      <p class="category-hero__desc">3Dプリンター・ロボット掃除機・空気清浄機・ドライヤー等の家電レビュー。</p>
      <span class="category-hero__count">{appliancePosts.length}件の記事</span>
    </div>
  </section>

  <section class="category-articles">
    <div class="container">
      {appliancePosts.length > 0 ? (
        <div class="articles__grid">
          {appliancePosts.map(post => (
            <ArticleCard
              title={post.data.title}
              description={post.data.description}
              slug={post.id}
              category={post.data.category}
              publishDate={post.data.publishDate}
              ogImage={'/images/og-default.svg'}
              type={post.data.type}
              productId={post.data.products?.[0]}
            />
          ))}
        </div>
      ) : (
        <p class="no-articles">記事はまだありません。</p>
      )}
    </div>
  </section>
</BaseLayout>

<style>
  .breadcrumb { font-size: .875rem; color: rgba(255,255,255,.8); margin-bottom: .5rem; }
  .breadcrumb a { color: rgba(255,255,255,.9); text-decoration: underline; }

  .category-hero {
    padding: 3rem 0 2.5rem;
    color: white;
  }
  .category-hero--appliance {
    background: linear-gradient(135deg, #065f46 0%, #059669 50%, #34d399 100%);
  }
  .category-hero__title { font-size: clamp(1.75rem, 4vw, 2.5rem); font-weight: 800; margin-bottom: .75rem; color: white; }
  .category-hero__desc { font-size: 1.05rem; color: rgba(255,255,255,.85); margin-bottom: 1rem; }
  .category-hero__count { background: rgba(255,255,255,.2); border: 1px solid rgba(255,255,255,.3); padding: .25em .8em; border-radius: 99px; font-size: .875rem; font-weight: 600; }

  .category-articles { padding: 3rem 0; }
  .articles__grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 1.75rem;
  }
  .no-articles { color: var(--color-text-muted); text-align: center; padding: 3rem 0; }

  @media (max-width: 640px) {
    .articles__grid { grid-template-columns: 1fr; }
    .category-hero { padding: 2rem 0 1.75rem; }
  }
</style>
